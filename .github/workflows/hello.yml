name: Hello

on:
  workflow_dispatch:

jobs:
  bonjour:
    runs-on: ubuntu-latest
    steps:
      - name: Dire bonjour
        run: echo "Bonjour depuis GitHub Actions"

      - name: Décrire la machine
        run: |
          echo "Système  : $(uname -a)"
          echo "Dossier  : $(pwd)"
          echo "Node     : $(node --version)"
          echo "Docker   : $(docker --version)"

      - name: Lister le dépôt
        run: ls -la